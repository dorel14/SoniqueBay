# Configuration SoniqueBay - Fichier d'exemple des variables d'environnement

# === CONNEXION AU SERVEUR NAS ===
# Identifiants pour le montage CIFS du répertoire musical
NAS_USER=djdmd14
NAS_PASS=mc30314500

# === CONFIGURATION DE L'APPLICATION ===
# Chemin du répertoire musical (optionnel, défaut: /music)
MUSIC_PATH=/music

# Plateforme d'exécution (optionnel, défaut: auto-détection)
PLATFORM=docker

# === CONFIGURATION REDIS (optionnel, avec valeurs par défaut) ===
REDIS_URL=redis://localhost:6379/0

# === CONFIGURATION DE LA BASE DE DONNÉES ===
DATABASE_URL=sqlite:///./data/music.db

# === CONFIGURATION DES APIs ===
API_PORT=8001
RECOMMENDER_API_PORT=8002
FRONTEND_PORT=8080

# === CONFIGURATION DEBUG ===
DEBUG=false
LOG_LEVEL=INFO